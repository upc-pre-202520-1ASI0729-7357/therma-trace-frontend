[build]
  command = "ng build --configuration production"
  publish = "dist/therma-trace/browser"

[build.environment]
  SECRETS_SCAN_ENABLED = "false"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200